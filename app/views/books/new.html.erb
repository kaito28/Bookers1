<h1>New Book</h1>
<% if book.errors.any? %>
  <div>
    <ul style="color: red">
      <% book.errors.full_messages.each do |message| %>
       <h3><%= book.errors.count %> errors prohibited this obj from being saved</h3>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>

<%= form_with model: @book, local:true do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  <h4>Body</h4>
  <%= f.text_area :body %>
  <%= f.submit 'craete book' %>
<% end %>